name: Build Cross-Platform Binaries

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: excel-draw-tool-linux
            asset_name: excel-draw-tool-linux-amd64
          - os: windows-latest
            artifact_name: excel-draw-tool-windows
            asset_name: excel-draw-tool-windows-amd64.exe
          - os: macos-latest
            artifact_name: excel-draw-tool-macos
            asset_name: excel-draw-tool-macos-amd64

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # 使用最小化依赖减小打包体积
        pip install -r requirements.txt --no-cache-dir
        pip install pyinstaller
      env:
        # 禁用pip缓存以减小构建空间
        PIP_NO_CACHE_DIR: 1

    - name: Build with PyInstaller (Linux/macOS)
      if: runner.os != 'Windows'
      run: |
        pyinstaller --clean --noconfirm build.spec
        # 清理构建缓存
        rm -rf build/
        rm -rf __pycache__/

    - name: Build with PyInstaller (Windows)
      if: runner.os == 'Windows'
      run: |
        pyinstaller --clean --noconfirm build.spec
        # 清理构建缓存
        Remove-Item -Recurse -Force build -ErrorAction SilentlyContinue
        Remove-Item -Recurse -Force __pycache__ -ErrorAction SilentlyContinue
      shell: pwsh

    - name: Prepare artifact (Linux)
      if: runner.os == 'Linux'
      run: |
        cd dist
        # 使用最大压缩级别
        tar -czf ${{ matrix.artifact_name }}.tar.gz excel-draw-tool/
        ls -lh ${{ matrix.artifact_name }}.tar.gz
        cd ..

    - name: Prepare artifact (macOS)
      if: runner.os == 'macOS'
      run: |
        cd dist
        # 使用最大压缩级别
        tar -czf ${{ matrix.artifact_name }}.tar.gz excel-draw-tool/
        ls -lh ${{ matrix.artifact_name }}.tar.gz
        cd ..

    - name: Prepare artifact (Windows)
      if: runner.os == 'Windows'
      run: |
        cd dist
        # 使用最大压缩级别
        Compress-Archive -Path excel-draw-tool -DestinationPath ${{ matrix.artifact_name }}.zip -CompressionLevel Optimal
        Get-Item ${{ matrix.artifact_name }}.zip | Select-Object Name, Length
        cd ..
      shell: pwsh

    - name: Upload artifact (Linux/macOS)
      if: runner.os != 'Windows'
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.artifact_name }}
        path: dist/${{ matrix.artifact_name }}.tar.gz

    - name: Upload artifact (Windows)
      if: runner.os == 'Windows'
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.artifact_name }}
        path: dist/${{ matrix.artifact_name }}.zip

    - name: Upload Release Asset (Linux/macOS)
      if: startsWith(github.ref, 'refs/tags/') && runner.os != 'Windows'
      uses: softprops/action-gh-release@v2
      with:
        files: dist/${{ matrix.artifact_name }}.tar.gz
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Upload Release Asset (Windows)
      if: startsWith(github.ref, 'refs/tags/') && runner.os == 'Windows'
      uses: softprops/action-gh-release@v2
      with:
        files: dist/${{ matrix.artifact_name }}.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

